/**
céaton d'une variable var i
*affectation de la variable = 12;
*Le tout est une instruction js qui se termine normalement par "i"


*Typage dynamique (on donne la valeur > et en fonction de la syntaxe le js comprend de quelle type de donner on parle)
*Les types de données en js sont :

- string (chaîne de caractères)
- nombre
- boolean 
- primitive
- undefined

Cela correspond à ce qu'on appelle du typage dynamique.

*/

var i = 12; //Ceci est une instruction//

i = i + 3; //concaténation > en gros > j'affecte à "i" sa valeur donc "12" et un "3"

var j = true;

var k = "Hello World";

var l;

var l_k; /**Underscore à metttre obligatoire en JS */

console.log(typeof(i));
console.log(typeof(j));
console.log(typeof(k));
console.log(typeof(l));
console.log(typeof(l_k));